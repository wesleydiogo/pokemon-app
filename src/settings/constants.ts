export const TILE_SIZE = 64;

export const MAP_SIZE_X = TILE_SIZE * 20;
export const MAP_SIZE_Y = TILE_SIZE * 13;

export enum EDirection {
    UP = 'ArrowUp',
    DOWN = 'ArrowDown',
    LEFT = 'ArrowLeft',
    RIGHT = 'ArrowRight'
}